{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node -e \"const fs=require('fs'); for (const f of ['package-lock.json','yarn.lock']) fs.rmSync(f,{force:true}); const ua=process.env.npm_config_user_agent||''; if (ua && !/pnpm\\//.test(ua)) { console.error('Use pnpm instead'); process.exit(1); }\"",
    "build": "pnpm --filter @workspace/apex-premier-league run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm --filter @workspace/apex-premier-league run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
